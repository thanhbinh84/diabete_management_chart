import 'package:flutter_test/flutter_test.dart';
import 'package:integration_test/integration_test.dart';
import 'package:tech_challenge/core/enums.dart';

void main() {
  IntegrationTestWidgetsFlutterBinding.ensureInitialized();

  // testWidgets('Min button test', (tester) async {
  //   await tester.pumpWidget(const Routes());
  //   final minButton = find.text(Criteria.min.name);
  //   await tester.tap(minButton);
  //   await tester.pumpAndSettle();
  //   expect(find.textContaining(Criteria.min.name), findsOneWidget);
  // });
  //
  // testWidgets('Start button test', (tester) async {
  //   await tester.pumpWidget(const Routes());
  //   final startButton = find.text('Start');
  //   await tester.tap(startButton);
  //   await tester.pumpAndSettle();
  //   expect(find.textContaining('Select date'), findsOneWidget);
  // });
}
